{
  "name": "admin-panel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node backend/index.js",
    "build": "cd admin-front && yarn install && yarn build && cd .. && cd student-front && yarn install && yarn build && cd .. && cd backend && yarn install && cd .."
  }
}
