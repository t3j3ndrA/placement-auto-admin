{
  "name": "admin-panel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "cd backend && node index.js",
    "build": "cd admin-front && yarn install && yarn build && cd .. && cd student-front && yarn install && yarn build"
  }
}
